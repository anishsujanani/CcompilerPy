#include <stdio.h>

int main() {
	int a = 3;
	a++;
	a ++;
	a = a + 3;
	b = a++;
	c = ++a;
	b = a ++;
	c = ++ a;
	c--;
	int x = --c;
	ABC != 223;
	a = a++ + ++ a;
	if(c++ == -- a) {
		// do stuff
	}
	for(int i = 0; i < a++; i++) {
		int z = --c + a++;
	}
	return 0;
}
